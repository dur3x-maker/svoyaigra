# 🚀 Деплой на GitHub Pages

## 📋 Подготовка проекта

Ваш проект уже готов к деплою! Все пути относительные и совместимы с GitHub Pages.

### ✅ Что уже сделано:

- ✅ Все пути к файлам относительные (`data/animals.json`, `style.css`, `script.js`)
- ✅ Создан файл `.nojekyll` для корректной работы на GitHub Pages
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Поддержка изображений из JSON

## 📁 Структура проекта для GitHub Pages

```
Svoyaigra/
├── .nojekyll          # Отключает Jekyll на GitHub Pages
├── index.html         # Главная страница
├── style.css          # Стили
├── script.js          # Логика игры
├── animals.json       # JSON с вопросами
├── blog.json
├── books.json
├── cars.json
├── color.json
├── eng.json
├── logic.json
├── math.json
├── music.json
├── space.json
├── words.json
├── world.json
├── README.md          # Описание проекта
└── DEPLOY.md          # Инструкция по деплою
```

## 🔧 Шаги для деплоя

### 1. Создайте репозиторий на GitHub

1. Зайдите на [GitHub](https://github.com)
2. Нажмите **New repository**
3. Название: `svoyaigra` (или любое другое)
4. Выберите **Public**
5. НЕ добавляйте README, .gitignore или лицензию
6. Нажмите **Create repository**

### 2. Загрузите проект в репозиторий

Откройте терминал в папке проекта и выполните:

```bash
# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Своя игра"

# Подключите удалённый репозиторий (замените YOUR_USERNAME и YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

### 3. Включите GitHub Pages

1. Зайдите в настройки репозитория: **Settings**
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите:
   - Branch: **main**
   - Folder: **/ (root)**
4. Нажмите **Save**
5. Подождите 1-2 минуты

### 4. Готово! 🎉

Ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/
```

Например: `https://username.github.io/svoyaigra/`

## 🔍 Проверка работоспособности

После деплоя проверьте:

- ✅ Страница загружается
- ✅ Можно ввести имена игроков и начать игру
- ✅ Категории отображаются корректно
- ✅ Вопросы загружаются из JSON
- ✅ Изображения в вопросах показываются
- ✅ Игра работает на мобильном устройстве
- ✅ Очки начисляются/снимаются правильно
- ✅ Переходы между этапами работают

## 🐛 Возможные проблемы и решения

### Проблема: 404 ошибка при загрузке JSON

**Решение:** Убедитесь, что:
- Файл `.nojekyll` присутствует в корне проекта
- Все JSON-файлы загружены в корень репозитория (не в папку)
- Пути в `script.js` относительные: `animals.json` (без `data/`)

### Проблема: Изображения не загружаются

**Решение:** 
- Проверьте URL изображений в JSON-файлах
- Убедитесь, что используются внешние ссылки (https://)
- Проверьте консоль браузера (F12) на ошибки CORS

### Проблема: Страница не обновляется после изменений

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R)
2. Подождите несколько минут (GitHub Pages обновляется не мгновенно)
3. Проверьте, что изменения загружены в репозиторий

## 📱 Тестирование на мобильных

Откройте сайт на телефоне/планшете и проверьте:
- Кнопки достаточно большие для нажатия
- Текст читается без зума
- Нет горизонтального скролла
- Модальные окна помещаются на экран

## 🔄 Обновление проекта

Для обновления игры после изменений:

```bash
# Добавьте изменённые файлы
git add .

# Создайте коммит с описанием изменений
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

## 📊 Мониторинг

Проверить статус деплоя можно в разделе **Actions** вашего репозитория.

## 🎯 Готово к использованию!

Теперь вы можете:
- Делиться ссылкой с друзьями
- Играть с любого устройства
- Обновлять вопросы через JSON-файлы
- Добавлять новые категории

---

**Важно:** Все изменения в JSON-файлах требуют коммита и пуша в репозиторий для обновления на GitHub Pages.
