# Своя игра - Браузерная игра для детей 7-10 лет

## 📁 Структура проекта

```
svoyaigra/
├── index.html          # Основная структура игры
├── style.css           # Стили и адаптивный дизайн
├── script.js           # Игровая логика
├── animals.json        # Животные
├── blog.json           # Блогеры
├── books.json          # Литература
├── cars.json           # Машинки
├── color.json          # Цвета и формы
├── eng.json            # Английский
├── logic.json          # Логика и загадки
├── math.json           # Математика
├── music.json          # Музыка
├── space.json          # Космос
├── words.json          # Русский язык
├── world.json          # Мир вокруг
└── README.md
```

## 🎮 Возможности

- **3 этапа игры** с возрастающей сложностью
- **12 категорий** с вопросами разных уровней
- **Динамическая загрузка** вопросов из JSON
- **Поддержка изображений** в вопросах
- **Кот в мешке** (15% вероятность)
- **Адаптивный дизайн** для телефонов и планшетов
- **1-4 игрока**

## 📝 Формат JSON-файлов

Каждый JSON-файл содержит массив вопросов:

```json
[
  { "level": 1, "q": "Текст вопроса", "a": "Ответ" },
  { "level": 2, "q": "Вопрос с картинкой", "a": "Ответ", "img": "https://..." },
  { "level": 3, "q": "Сложный вопрос", "a": "Ответ" }
]
```

### Поля:
- `level` (1-3) - уровень сложности, соответствует этапам игры
- `q` - текст вопроса
- `a` - правильный ответ
- `img` (опционально) - URL изображения

## 🔧 Как добавить новую категорию

1. Создайте JSON-файл в корне проекта (например, `new_category.json`)
2. Добавьте категорию в `script.js`:

```javascript
const CATEGORIES = [
    // ...
    { id: 'new_category', title: 'Название', file: 'new_category.json' }
];
```

## 🚀 Запуск

### Локально:

Просто откройте `index.html` в браузере или запустите локальный сервер:

```bash
python -m http.server 8000
```

Затем откройте http://localhost:8000

### GitHub Pages:

Проект готов к деплою на GitHub Pages! Все пути относительные.

Смотрите подробную инструкцию в файле `DEPLOY.md`

После деплоя игра будет доступна по адресу:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/
```

## 📱 Адаптивность

Игра оптимизирована для:
- Десктопов (1200px+)
- Планшетов (768px-1200px)
- Телефонов (до 768px)

Крупные кнопки и текст обеспечивают удобство на сенсорных экранах.

## 🎯 Игровая механика

1. **Этапы**: Игра проходит 3 этапа с разными ценами вопросов
2. **Категории**: На каждом этапе выбираются 4 случайные категории
3. **Уровни**: Вопросы автоматически подбираются по уровню этапа
4. **Попытки**: На каждый вопрос даётся 2 попытки
5. **Очки**: За правильный ответ начисляются, за неправильный снимаются

## 🛠️ Технологии

- Чистый HTML5
- CSS3 с Flexbox/Grid
- Vanilla JavaScript (ES6+)
- Fetch API для загрузки JSON
